// module.exports.create_global_parameters = function() {

    // var controller_id = '00000000000000000000000000000002';

    // var current_cycle_num = 3;

    // var cycleDuration = 6500;
    // var syDuration = 1000;
    // var rtDuration = 5000;
    // var ppDuration = cycleDuration - syDuration - rtDuration;

    // var priority_value_rule = new Map();
    // priority_value_rule.set('+', 5); // 5 pts for scheduling a compulsory task (*)
    // priority_value_rule.set('*', 2); // 2 pts for scheduling an optional task (+)
    // priority_value_rule.set(' ', 0); // 0 pt for scheduling no task ( )

    // var allocation_rule = [[1,4,5],[2,6,7],[3,8,9]]; // At each slot, we have a list of controllers that are allowed to occupy the slot.
    // var allocation_rule_version = 42;

    // var num_probing_slots = 4;
    // var num_sy_slots = 4;

    // var rt_reward = 5;

    // global.controller_id = controller_id;
    // global.priority_value_rule = priority_value_rule;
    // global.current_cycle_num = current_cycle_num;
    // global.cycleDuration = cycleDuration;
    // global.syDuration = syDuration;
    // global.rtDuration = rtDuration;
    // global.ppDuration = ppDuration;
    // global.allocation_rule = allocation_rule;
    // global.allocation_rule_version = allocation_rule_version;
    // global.num_probing_slots = num_probing_slots;
    // global.num_sy_slots = num_sy_slots;
    // global.rt_reward = rt_reward;
// }
